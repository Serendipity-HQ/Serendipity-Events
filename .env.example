# Required — free key from https://developer.ticketmaster.com/
TICKETMASTER_API_KEY=

# Optional — enables the Claude enrichment layer (categories, vibes, blurbs).
# Without it, ingestion still runs; enrichment is skipped.
ANTHROPIC_API_KEY=
